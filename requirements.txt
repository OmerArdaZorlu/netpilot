# --- core runtime ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pyyaml==6.0.2
rich==13.9.4

# --- akış optimizasyonu (flowopt.py) ---
# Çok mallı akış problemi doğrusal program olarak kuruluyor; HiGHS çözücüsü
# scipy içinde geliyor. numpy scipy'nin zaten bağımlılığı, açıkça yazıldı.
numpy>=1.26
scipy>=1.11

# --- canlı mod (faz 2) ---
# Hacim beslemesi: paket yakalama. Npcap sürücüsü ayrıca kurulu olmalı.
# ⚠️ Yakalama arayüzü ve yetki: Npcap "non-admin" kurulmadıysa yönetici gerekir.
scapy==2.6.1
# Kimlik beslemesi: 5'li -> PID -> süreç adı (bağlantı tablosu yoklaması).
psutil>=5.9
